function decimalNaN() {
  if (game.downloads.gte(new Decimal("ee1024"))) {
    alert("Un-naned for thanks while do what? when so NaN first to ee1024 downloads have now.")
    console.error("UN-NANED!")
    console.error("Unknown Downloads.")
    downloads = NaN
  }
}

function UpdateDecimalArea() {
  console.log("cool")
  if (game.downloads.gte("ee9e15")) {
     decimalNaN();
  }
}

setInterval(decimalNaN, 100)
setInterval(UpdateDecimalArea, 100)